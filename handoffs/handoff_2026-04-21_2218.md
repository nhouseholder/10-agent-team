# Handoff — feature-install-surface — 2026-04-21 22:18
**Repo:** nhouseholder/8-agent-team | **Branch:** feature/install-surface-foundation | **Last commit:** 85601bb 2026-04-21 22:16:50 -0700
**Local = Remote:** yes

---

## 1. Session Summary
The session finalized and pushed the `v1.7.0` install/runtime branch for the 8-agent OpenCode framework. That release adds a real install surface, dependency probing, scratch-profile smoke tests, a reasoning benchmark harness, and a model-neutral runtime contract where repo-managed agents inherit the active OpenCode session model. After the branch push, the recurring OpenCode config failure was traced to the separate main repo checkout reintroducing a top-level `models` block; that local regression was patched and the live installed config at `~/.config/opencode/opencode.json` was re-synced and verified to parse cleanly.

## 2. What Changed
- **Install/runtime productization**: `package.json`, `INSTALL.md`, `.github/workflows/validate.yml`, `scripts/check-deps.js`, `scripts/install-opencode.js`, `scripts/smoke-test-install.js` — added a supported install surface with CI, dependency checks, and scratch install verification.
- **Behavioral evals**: `scripts/reasoning-eval.js`, `scripts/benchmark-reasoning.js`, `tests/golden/reasoning-cases.js` — added executable reasoning benchmarks and wired them into package scripts and CI.
- **Dynamic model inheritance**: `opencode.json`, `oh-my-opencode-slim.jsonc`, `examples/*.json`, `scripts/validate-agents.js`, `scripts/validate-reasoning-scenarios.js`, `spec/models.yaml` — removed shipped model pinning and aligned validators/examples with a model-neutral default runtime.
- **Docs and release surface**: `README.md`, `docs/README.md`, `docs/ARCHITECTURE.md`, `docs/AGENT-REFERENCE.md`, `docs/TROUBLESHOOTING.md`, `docs/MODEL-SELECTION.md`, `CHANGELOG.md` — updated the public contract, troubleshooting, and release notes for `v1.7.0`.
- **Live runtime sync**: `~/.config/opencode` — replaced the stale full repo clone with installer-managed runtime assets and verified `opencode --help` succeeds. Backup kept at `~/.config/opencode.backup-20260421-220217`.
- **Local main repo regression fix**: `/Users/nicholashouseholder/ProjectsHQ/8-agent-team/{opencode.json,examples/minimal.json,examples/openrouter-council.json,scripts/validate-reasoning-scenarios.js,docs/ARCHITECTURE.md}` — removed the reintroduced unsupported `models` block and restored the dynamic model-inheritance contract locally.

## 3. In Progress / Blocked
**In progress:** The feature branch is complete and pushed. The separate main repo checkout now has a local uncommitted fix for the recurring `models` regression in `opencode.json` and related files; decide whether to cherry-pick/merge `feature/install-surface-foundation` into `main` or discard that local drift after merging.
**Blocked:** Nothing blocked.

## 4. Next Steps (Prioritized)
1. Merge or cherry-pick `feature/install-surface-foundation` into `main` so the default repo surface matches the pushed `v1.7.0` runtime contract and the local main checkout no longer diverges.
2. Restart OpenCode and verify one UI pass: choose a non-Qwen model, switch across orchestrator plus one specialist, and confirm the UI no longer snaps repo-managed agents back to Qwen.
3. If extra names such as Octto or Commander still appear after restart, treat them as OpenCode-owned built-ins/runtime capabilities and document that they are outside the repo-managed 8-agent roster.

## 5. Warnings
The pushed work lives on `feature/install-surface-foundation`, not `main`. The separate main checkout at `/Users/nicholashouseholder/ProjectsHQ/8-agent-team` is now dirty with a local-only fix to the reintroduced `models` regression. Also, OpenCode appears to persist the currently selected session model into the installed live config, so a concrete `model` value in `~/.config/opencode/opencode.json` does not by itself prove the repo is hardcoding it.

## 6. For The Next Agent
Read first: (1) this handoff, (2) `/Users/nicholashouseholder/ProjectsHQ/8-agent-team/AGENTS.md`, (3) `~/.claude/anti-patterns.md`
**Canonical path:** `/Users/nicholashouseholder/ProjectsHQ/8-agent-team/.worktrees/feature-install-surface`