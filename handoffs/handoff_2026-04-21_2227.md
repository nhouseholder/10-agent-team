# Handoff — 8-agent-team — 2026-04-21 22:27
**Repo:** nhouseholder/8-agent-team | **Branch:** main | **Last commit:** 27d4c6a 2026-04-21 22:26:56 -0700
**Local = Remote:** yes

---

## 1. Session Summary
The user asked to finish the main-repo release loop: commit, push, bump, sync, deploy, and write a handoff. This session cut and pushed `v1.7.1` on `main` as a patch release to fix a reintroduced OpenCode config regression where `opencode.json` had an unsupported top-level `models` block again. The live installed config at `~/.config/opencode/opencode.json` was verified to parse cleanly with `opencode --help`, and the repo validators now accept the dynamic model-inheritance contract on main.

## 2. What Changed
- **Patch release**: `CHANGELOG.md`, `README.md`, `docs/README.md`, `examples/{standard,with-memory,enterprise,minimal}.json` — bumped visible release surfaces to `1.7.1` and documented the regression fix.
- **Config regression fix**: `opencode.json` — removed the unsupported top-level `models` block and removed reintroduced council agent-level model overrides from the shipped default config.
- **Examples/docs alignment**: `examples/minimal.json`, `examples/openrouter-council.json`, `examples/standard.json`, `examples/with-memory.json`, `examples/enterprise.json`, `docs/ARCHITECTURE.md` — restored the dynamic session-model contract and removed stale pinned-model examples from the shipped defaults.
- **Validator alignment**: `scripts/validate-reasoning-scenarios.js`, `scripts/validate-agents.js` — updated main-repo validation so the default config may omit `model` while still rejecting top-level `models` and default agent-level overrides.
- **Release sync**: committed `27d4c6a`, pushed `main`, and pushed tag `v1.7.1`.

## 3. In Progress / Blocked
**In progress:** All requested main-repo release work is complete. The separate feature branch `feature/install-surface-foundation` still exists remotely with the larger install-surface productization work and its own handoff trail.
**Blocked:** No deploy target exists in this repo. There is no `package.json`, no `.github/workflows/`, no `wrangler.toml`, and no Pages/Workers surface on `main`, so there was nothing real to deploy.

## 4. Next Steps (Prioritized)
1. Merge or cherry-pick the already-pushed `feature/install-surface-foundation` work into `main` if you want the install scripts, benchmark harness, and installer-backed smoke path on the main branch too.
2. Restart OpenCode and verify the UI once with a non-Qwen model to confirm the main-repo default config no longer reintroduces the `models` error or snap-back behavior.
3. If you want a real deploy step in future sessions, add an actual deploy target to `main` first (for example a CI workflow or a runtime package/build surface); this repo currently has no production deployment artifact on main.

## 5. Warnings
The feature branch and main branch now diverge materially. `main` has the `v1.7.1` regression fix, while `feature/install-surface-foundation` carries the larger install/runtime productization work from earlier in the night. Also, OpenCode appears able to persist the currently selected session model into the installed live config, so seeing a concrete `model` value in `~/.config/opencode/opencode.json` does not necessarily mean the repo has reintroduced hardcoded model behavior.

## 6. For The Next Agent
Read first: (1) this handoff, (2) `/Users/nicholashouseholder/ProjectsHQ/8-agent-team/AGENTS.md`, (3) `~/.claude/anti-patterns.md`
**Canonical path:** `/Users/nicholashouseholder/ProjectsHQ/8-agent-team`